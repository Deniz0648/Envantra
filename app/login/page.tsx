"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { LockKeyhole } from "lucide-react";

export default function LoginPage(){const router=useRouter();const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=new FormData(e.currentTarget);const response=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username:data.get("username"),password:data.get("password")})});setBusy(false);if(!response.ok){setError("Kullanıcı adı veya parola hatalı.");return}const body=await response.json() as {data?:{next?:string}};router.push(body.data?.next??"/select");router.refresh()}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20,background:"#142126"}}><form onSubmit={submit} style={{width:"100%",maxWidth:390,background:"white",borderRadius:16,padding:30,boxShadow:"0 24px 60px #07101480"}}><div className="brand-mark" style={{marginBottom:20}}><LockKeyhole size={17}/></div><div className="eyebrow">Diler Holding</div><h1 style={{margin:"6px 0",fontSize:26}}>Envantra’ya giriş</h1><p style={{color:"#68747d",fontSize:13,margin:"0 0 24px"}}>Yetkili kullanıcı hesabınızla devam edin.</p><label style={{fontSize:11,fontWeight:700}}>Kullanıcı adı<input name="username" required autoComplete="username" style={{display:"block",width:"100%",height:42,border:"1px solid #e5e8e8",borderRadius:8,padding:"0 11px",margin:"6px 0 15px"}}/></label><label style={{fontSize:11,fontWeight:700}}>Parola<input name="password" type="password" required autoComplete="current-password" style={{display:"block",width:"100%",height:42,border:"1px solid #e5e8e8",borderRadius:8,padding:"0 11px",margin:"6px 0 10px"}}/></label>{error&&<p role="alert" style={{fontSize:11,color:"#b52d1b"}}>{error}</p>}<button className="primary" style={{width:"100%",justifyContent:"center",marginTop:14,height:43}} disabled={busy}>{busy?"Giriş yapılıyor…":"Giriş yap"}</button></form></main>}
